/**
 * Optional v2: replace hero with lifestyle / macro video or short loop.
 * See plan todo content-media.
 */
export function OptionalMediaNote() {
  return (
    <p className="mt-4 text-[11px] text-zinc-600">
      <span className="text-zinc-500">Media note:</span> Hero uses a placeholder SVG.
      Drop in macro pack shots or a silent looping clip for stronger conversion.
    </p>
  );
}
